global:
  scrape_interval: 15s
  scrape_timeout: 5s
  evaluation_interval: 15s

rule_files:
  - /etc/prometheus/alerts.yml

scrape_configs:
  - job_name: openslack_workflow_control_shadow
    metrics_path: /metrics
    static_configs:
      - targets: [app:8080]
