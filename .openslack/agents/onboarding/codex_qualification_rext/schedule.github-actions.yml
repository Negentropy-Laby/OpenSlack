name: OpenSlack Agent Tick - codex_qualification_rext

on:
  schedule:
    # Off-peak minutes: 7, 22, 37, 52 past each hour (4x/hour)
    - cron: '7,22,37,52 * * * *'
  workflow_dispatch:

concurrency:
  group: openslack-agent-codex_qualification_rext
  cancel-in-progress: false

permissions:
  contents: write
  issues: write
  pull-requests: write
  actions: read

jobs:
  tick:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
        with:
          repository: 'Negentropy-Laby/OpenSlack'
          token: ${{"{{ secrets.OPENSLACK_GITHUB_TOKEN }}"}}
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest
      - run: bun install --frozen-lockfile
      - run: bun run openslack workspace validate
      - run: bun run openslack agent bootstrap --agent-id "codex_qualification_rext"
      - run: bun run openslack agent tick --agent-id "codex_qualification_rext" --claim-one --source github-project
