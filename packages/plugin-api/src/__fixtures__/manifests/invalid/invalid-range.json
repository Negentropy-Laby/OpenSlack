{
  "schema": "openslack.plugin.v1",
  "id": "invalid-range",
  "version": "1.0.0",
  "name": "Invalid host version range",
  "requires": { "openslack": "definitely-not-a-range" },
  "gate": { "mode": "SHADOW", "gateId": "host.read-only" },
  "capabilities": ["host.actions.read"],
  "contributes": [
    {
      "kind": "action_alias",
      "id": "status",
      "target": { "kind": "host_action", "id": "status.show" }
    }
  ]
}
