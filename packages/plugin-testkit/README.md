# @openslack/plugin-testkit

Public, stable preflight checks for declarative OpenSlack plugin packages.

The testkit reports compatibility and integrity findings. The Red host independently revalidates every plugin before activation.
