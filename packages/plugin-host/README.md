# @openslack/plugin-host

Governed, instance-scoped host for declarative OpenSlack plugin manifests.

Every host requires an explicit policy adapter and sealed target binding. The package does not provide approval, merge, shell, or arbitrary third-party code execution.
