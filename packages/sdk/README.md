# @openslack/sdk

Convenience exports and builders for OpenSlack's stable declarative plugin contracts.

Use `@openslack/plugin-host` separately when an embedding application needs governed activation and routing.
