import React from 'react';

/**
 * Stub for ErrorOverview — the full version depends on code-excerpt
 * and stack-utils which are not available in this package.
 * Conditionally used in App.tsx only when an error boundary triggers.
 */
export default function ErrorOverview(_props: { error: Error }) {
  return null;
}
